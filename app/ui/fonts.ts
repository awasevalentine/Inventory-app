import {Inter, Lato, Lusitana} from 'next/font/google'

export const inter = Inter({subsets: ['greek']})
export const lato = Lato({weight: ['700', '900'], subsets: ['latin']})
export const lusitana = Lusitana({weight: ['400', '700'], subsets: ['latin']})